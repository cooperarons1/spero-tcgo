node_modules/
dist/
.firebase/
*.log
.DS_Store
.env
.env.*
.env.local

# Phase 4 — generated training data and per-shard JSONLs (huge, regenerated
# from scripts/parallel-simulate.ts on demand). The compact, ship-target
# weights JSON IS committed; the multi-hundred-MB raw snapshots are not.
data/sim-history*.jsonl
data/sim-history*.jsonl.*
data/teacher-decisions.jsonl
# Balance audit raw JSONLs (huge — 80k games ≈ 1.7 GB). Keep only the
# aggregated markdown in docs/ under git; regenerate the JSONL via
# scripts/parallel-simulate.ts --teacher-vs-teacher.
data/balance-audit-*.jsonl
# Alternate teacher label files (e4b / 31b / future teachers) — kept as
# local backups; the canonical teacher-labels.jsonl is also gitignored.
data/teacher-labels.e4b.bak.jsonl.*
data/teacher-labels.31b.jsonl
data/teacher-labels.jsonl
data/teacher-labels-smoke.jsonl
data/teacher-queue.jsonl
data/teacher-labels.llama-contaminated.bak.jsonl
data/teacher-labels-smoke.llama-contaminated.bak.jsonl
data/teacher-labels.e4b.bak.jsonl

# Animation model preview frames (regenerated from scripts/animation-model)
data/animation-preview/
# Animation model training JSONL + checkpoint (regenerated)
data/animation-training.jsonl
data/animation-training.jsonl.bak
data/animation-checkpoint.json

# Python venv created by the trainer
.venv/

# Python bytecode caches
__pycache__/
*.pyc

data/soak/
